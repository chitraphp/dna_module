<?php
function dna_program($input) {
  $characters = ["A", "T", "C", "G"];
  $replace_chars = ["T", "A", "G", "C"];

  $second_half_dna = strtr($input, $characters, $replace_chars);
  return $output;
}
